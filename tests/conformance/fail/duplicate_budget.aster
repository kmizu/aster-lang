module fail.duplicate_budget;

agent A() requires [] {
  state {}
  budget per_event { model_calls <= 1; model_calls <= 2; }
  on message(msg: Incoming<Text>) -> Result<Unit, Error> {
    return Ok(Unit);
  }
}
